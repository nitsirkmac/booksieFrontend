// import { Link } from "react-router-dom"
import { useState } from "react"
import { Quote } from '../components/appStyles'

function Index({  }) {


    return (
        <section>
            <Quote>“A reader lives a thousand lives before he dies . . . The man who never reads lives only one.” - George R.R. Martin"</Quote>
        </section>
    )
}

export default Index